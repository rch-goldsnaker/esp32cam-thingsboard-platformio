#ifndef SECRETS_H
#define SECRETS_H

// Copy this file to "secrets.h" (same directory) and fill in your real values.
// secrets.h is gitignored, do NOT commit it.

#define WIFI_SSID_TXT     "your_wifi_ssid"
#define WIFI_PASSWORD_TXT "your_wifi_password"
#define TB_TOKEN_TXT      "your_thingsboard_device_token"

#endif
